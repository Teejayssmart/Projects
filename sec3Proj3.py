# Write a program that uses input to prompt a user
# for their name and then prints to the
# console the number of characters in the inputed name.
#use len to find the lenght of string

name = input("What is your name: ")
print(len(name))

